@{
    ViewData["Title"] = "State Add/Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@model MultiAddressBook.Areas.LOC_State.Models.LOC_StateModel;

<div class="row">
    <div class="col-md-12">
        <h2>@TempData["LOC_State_Insert_Msg"]</h2>

        <div class="container">
            <h2>State</h2>
            <hr />
            <form method="post" asp-area="LOC_State" asp-controller="LOC_State">

                @Html.HiddenFor(x => x.StateID)

                <div class="form-group mb-7">
                    <label><span class="text-danger">*</span>Country</label>
                    <select class="form-select rounded-pill" data-control="select2" data-placeholder="Select Country" data-allow-clear="true" asp-for="CountryID" asp-items="@(new SelectList(ViewBag.CountryList, "CountryID", "CountryName"))">
                        <option value=""></option>
                    </select>
                    <span asp-validation-for="CountryID" class="text-danger"></span>
                </div>

                <div class="form-floating mb-7">
                    <input type="text" class="form-control rounded-pill" id="floatingInput" asp-for="StateName" placeholder="Gujarat" />
                    <label for="floatingInput"><span class="text-danger">*</span>State</label>
                    <span asp-validation-for="StateName" class="text-danger"></span>
                </div>

                @Html.HiddenFor(x => x.CreationDate)
                @Html.HiddenFor(x => x.ModificationDate)
                <input type="hidden" class="form-control" asp-for="UserID" value="@ViewBag.UserID" />

                <div class="row">
                    <div class="col-4">
                        <button type="submit" class="btn btn-success rounded-pill" asp-action="Save">Save</button>
                        &nbsp;
                        <a class="btn btn-danger rounded-pill" asp-action="Index">Cancel</a>
                    </div>
                </div>
            </form>
        </div>
    </div>

</div>

@section Scripts{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}